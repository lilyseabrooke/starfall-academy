import LoadingScreen from "@/components/LoadingScreen";

// Shown while the character sheet's server data (sheet + party) is fetched.
export default function Loading() {
  return <LoadingScreen />;
}
