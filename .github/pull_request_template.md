## Summary

<!-- What broke, or what's missing, and for whom (a player, the GM, a specific
character/campaign if that's how it was found)? Keep it to the "why" — the
diff already shows the "what". -->

## Changes

<!-- The actual changes, as a short list. Call out anything a reviewer
should specifically double check (a migration, an RLS policy, a data
backfill). -->

## Version

<!-- See VERSIONING.md. Every PR merged to main bumps package.json's
"version" and gets a CHANGELOG.md entry — pick one: -->

- [ ] **Patch** — bug fix, copy/UI tweak, or a refinement to something already shipped this minor version
- [ ] **Minor** — a new player- or GM-facing feature
- [ ] **Major** — a save-data migration or rules-breaking change (rare)

New version: `vX.Y.Z`
`package.json` and `CHANGELOG.md` updated to match.

## Testing

<!-- tsc, eslint, manual verification, migration checks against row counts —
whatever actually applies. -->
