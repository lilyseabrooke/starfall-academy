-- Placeholder table from the pre-launch coming-soon page (e9b78fc). The
-- landing page was replaced with The Portal (dc27655) and hasn't read
-- announcements since; nothing in the app references this table anymore.
-- Full row backup taken before this drop (2 rows, both boilerplate copy).
drop table if exists public.announcements;
